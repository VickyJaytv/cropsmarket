export enum Role {
  BUYER = "buyer",
  FARMER = "farmer",
  ADMIN = "admin",
}

export enum Availability {
  IN_STOCK = "in stock",
  SOLD_OUT = "sold out",
}

export enum Status {
  ACTIVE = "active",
  SUSPENDED = "suspended",
}

export enum ListingStatus {
  ACTIVE = "active",
  SOLD = "sold",
  PAUSED = "paused",
}

export enum AccountType {
  INDIVIDUAL = "INDIVIDUAL",
  BUSINESS = "BUSINESS",
}
